针对您想通过在 DOM 节点绑定同一个 Class 来控制深浅主题的需求，目前的系统已经具备了基础架构（通过 useTheme.ts【src\composables\useTheme.ts】 在 html 标签上切换 dark 类名）。

为了进一步扩展自定义颜色变量，您可以按照以下方案进行优化和定义：

### 1. 变量定义位置
建议在 variables.scss（src\assets\css\variables.scss） 中统一管理。

- 默认变量（浅色） ：定义在 :root 下。
- 深色变量 ：定义在 html.dark 下。

### 2. 具体的定义方式
您可以采用“语义化变量”映射到“基础颜色值”的方式。这样在业务代码中只需要使用语义化变量。如：

// src/assets/css/variables.scss

:root {
  /* 1. 先定义基础色盘 (不变的) */
  --color-blue-500: #3b82f6;
  --color-gray-100: #f3f4f6;
  --color-gray-900: #111827;

  /* 2. 定义语义化变量 (随主题变化的) - 默认浅色 */
  --my-custom-bg: var(--color-gray-100);
  --my-custom-text: var(--color-gray-900);
  --my-brand-color: var(--color-blue-500);
}

/* 3. 深色模式下的变量重写 */
html.dark {
  --my-custom-bg: var(--color-gray-900);
  --my-custom-text: var(--color-gray-100);
  // 如果品牌色在深色下需要调整
  --my-brand-color: #60a5fa; 
}

### 3. 如何控制与绑定
目前的 useTheme.ts 【src\composables\useTheme.ts】 已经实现了逻辑：

- 当 isDark 为 true 时，执行 document.querySelector("html")?.classList.add("dark") 。
- 这意味着整个项目的根节点 <html> 会带上 .dark 类名。
### 4. 在业务组件中使用
您在任何 Vue 组件或 CSS 文件中，只需要引用定义的变量名即可，无需关心当前是什么主题，如下代码：
<template>
  <div class="my-box">
    这是一段会自动切换颜色的文字
  </div>
</template>

<style scoped lang="scss">
.my-box {
  background-color: var(--my-custom-bg); // 自动切换
  color: var(--my-custom-text);           // 自动切换
  border: 1px solid var(--my-brand-color);
}
</style>

总结建议
统一 Class 名称：建议沿用目前的 .dark。因为 Element Plus 等主流 UI 库默认也是识别 html.dark 来实现暗黑模式的，这样可以保持兼容。
文件组织：
基础变量：放在 variables.scss（src\assets\css\variables.scss）。
控制逻辑：保持在 useTheme.ts【src\composables\useTheme.ts】 。
全局应用：保持在 App.vue（src\App.vue） 中调用 useTheme()。
这种方案的优点是：一次定义，全域生效。您只需要关注 html 标签上是否有 .dark，而样式逻辑完全交给 CSS 变量去处理。